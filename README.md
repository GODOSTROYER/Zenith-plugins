# Zenith integrations

**Your stack, clearly in view. In the tools where you build.**

This repository is the distribution home for Zenith's standalone MCP connector and native Codex and Claude Code packages. The application and its operational authority live in [`GODOSTROYER/zenith`](https://github.com/GODOSTROYER/zenith).

## Development status

The repository has been initialized for a reviewable implementation. Development changes will arrive on an isolated branch and draft pull request; this initial commit is not an installable release.

The earlier conversation's downloadable ZIP was inspected on September 12, 2026 and contained no files. No implementation or test claims from that handoff should be treated as release evidence. The implementation branch will record commands actually run, their results, and outstanding acceptance gates.

No package has been published, no production infrastructure has been deployed, and the repository's private visibility is unchanged.
